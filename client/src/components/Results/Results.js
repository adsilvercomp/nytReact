import React from "react";


const Results = (props) => (
            
        props.articles.map(article => (

            <h3>{article}</h3>
            
        ))
        
            
    )

    












export default Results;
